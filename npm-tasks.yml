---
- name: create nodesource repo
  command: curl -sL https://rpm.nodesource.com/setup_6.x | sudo -E bash -
- name: Yum install nodejs
  yum:
    name: nodejs
    state: latest
    enablerepo: nodesource
